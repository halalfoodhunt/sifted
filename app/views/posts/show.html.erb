<%= render 'layouts/header' %>
<br>
<div class="container">
  <ol class="breadcrumb">
    <li><%= link_to 'Home', root_path %></li>
    <li class="active"><a href="#"><%= @post.url_slug %></a></li>
  </ol>
  <div class="col-sm-12 col-md-12 padding-0">
    <div class="col-xs-12 col-md-9 padding-0" style="border-right: 1px solid #ccc;">
      <div class="col-sm-12 col-md-12">
        <h1><%= @post.url_slug %></h1>
          <div class="byline"><i>by</i> <%= @post.user.name %> <%= @post.date_published.strftime("%b %d, %Y") %></div>
          <br><center><%= image_tag @post.featured_image.url(:large), :class => "img-responsive", :style =>"width:100%;" %>
        </center>
        <p><%= @post.content.html_safe %></p>
      </div>
      <div class="col-sm-12 col-md-12">
        <h4>Share</h4>
        <%= social_share_button_tag("My Home Page")%>
      </div>
      <br>
      <div class="col-sm-12 col-md-12 padding-0">
        <h4>More Articles</h4>
        <%= render 'post' %>
      </div>
      <br>
    </div>
    <div class="col-xs-12 col-md-3">
    <% @ads.each do |ad| %>
      <%= image_tag ad.image.url(:square), :class => "img-responsive", :style =>"width:100%;" %>
    <% end %>
    </div>
  </div>
</div>