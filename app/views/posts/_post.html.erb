<% @posts.each do |post| %>
        <div class="col-sm-6 col-md-4">
          <div class="thumbnail">
            <%= image_tag post.featured_image.url(:square), :class => "img-responsive" %>
            <div class="caption">
              <h5><%= post.title %></h5>
              <p><%= link_to 'Read More', post.article_url, :class => "btn btn-default" %></p>
            </div>
          </div>
        </div>
        <% end %>