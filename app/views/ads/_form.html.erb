<%= bootstrap_form_for(@ad) do |f| %>
  <% if @ad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ad.errors.count, "error") %> prohibited this ad from being saved:</h2>

      <ul>
      <% @ad.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= image_tag(@ad.image.url(:square), :class => "img-responsive") %><br>
    <%= f.file_field :image, hide_label: true %>
  </div>
  <div class="field">
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.date_select:commencement_date %>
  </div>
  <div class="field">
    <%= f.date_select :expiry_date %>
  </div>
  <div class="field">
    <%= f.check_box :publish %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
